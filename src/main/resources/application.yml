server:
  port: 8089

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/jwtstudy?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.datealive.pojo

